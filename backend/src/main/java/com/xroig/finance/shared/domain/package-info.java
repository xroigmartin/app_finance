/**
 * Shared kernel: value objects and domain primitives reused across every bounded
 * context (money, date ranges, typed-id contract and the domain exception
 * hierarchy). Pure domain — no Spring, no JPA. Enforced by {@code ArchitectureTest}.
 */
package com.xroig.finance.shared.domain;
