/**
 * Budgets bounded context — infrastructure layer: persistence adapters (JPA entities,
 * Spring Data repositories, mappers, the command and read adapters and the cross-context
 * existence/catalog adapters) and the inbound web adapter.
 */
package com.xroig.finance.budgets.infrastructure;
