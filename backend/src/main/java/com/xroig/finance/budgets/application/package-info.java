/**
 * Budgets bounded context — application layer: inbound ports (use cases), the
 * application service and the read-side query port + read models. Depends on the
 * domain, never on infrastructure.
 */
package com.xroig.finance.budgets.application;
