# During the incremental migration, the layer rules target the new hexagonal
# packages (..domain.. / ..application.. / ..infrastructure..). Until every domain
# is migrated some rules match no classes yet; do not fail on an empty match.
archRule.failOnEmptyShould=false
