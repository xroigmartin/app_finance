# The migration is complete: every layer rule matches real classes, so a rule that
# suddenly matches nothing (e.g. a context deleted or mis-packaged) should fail rather
# than pass silently.
archRule.failOnEmptyShould=true
